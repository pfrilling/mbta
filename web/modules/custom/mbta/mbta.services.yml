services:
  mbta.api:
    class: Drupal\mbta\MbtaApi
    arguments: ['@cache.mbta']

  cache.mbta:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory: cache_factory:get
    arguments: [mbta]
